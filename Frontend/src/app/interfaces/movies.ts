export interface Movie {
  id_movie?: number,
  genre: string,
  clasification: string,
  format: string, 
  description: string,
  durationMin: number,
  title: string,
  imageUri: string //url de la imagen
}