export interface Movie {
    id_movie?: number,
    title: string,
    genre: string,
    format: string, 
    description: string,
    clasification: string,
    durationMin: number,
    image: File
}