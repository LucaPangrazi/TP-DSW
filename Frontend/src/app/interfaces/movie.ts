export interface Movie {
    id_movie?: number;//? porque es opcional
    title: string,
    genre: string,
    format: string, 
    description: string,
    clasification: string,
    durationMin: number
}